Generic single-database configuration.

alembic revision --autogenerate -m "mensagem_da_migracao"

alembic upgrade head

alembic history
alembic current
alembic heads
alembic downgrade <versao>

SELECT setval('negociacao_credito_id_seq', 381);

SELECT setval('parcelamento_negociacao_pkey', );